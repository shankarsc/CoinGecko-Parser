CoinGecko Parser contains 5 functions: get_price(), get_volume(), get_market_cap(), get_daily_change(), get_prct_change()
Returns the data in a DataFrame format
Currently, only one ticker is able to be queried at once